# UART

El microcontroaldor (como la mayoría) tiene periféricos UART ("Transmisor/Receptor Asíncrono Universal"). Hay dos tipos de periféricos UART en la MB2: el más antiguo `UART` y el más nuevo `UARTE` ("UART con Easy DMA"). Usaremos un periférico `UARTE` para comunicarnos con el puerto serie.

A través de este capítulo, usaremos la comunicación serie para intercambiar información entre el microcontrolador y el ordenador.