# Mi solución

Aquí está una posible solución (`src/main.rs`). Tendremos en cuenta que se pueden obtener valores de G bastante altos golpeando el borde de la MB2 contra una mesa. Esto podría romper el acelerómetro, así que quizá sea mejor no intentarlo, ¡Allá tú!.

``` rust
{{#include src/main.rs}}
```
